{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
     "format": {
      "cache": false
    },
    "dev": {
      "cache": false
    },
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"]
    }
  }
}
